import React, {useState} from "react";

function UserDropdown(props)
{
    return(
        <div className = "dashboard-page-header-user">
            PIHJISOLASIOAS:IAS
        </div>
    );
}

export default UserDropdown;